<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="OrchidPro.Views.Pages.Locations.LocationsEditPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:OrchidPro.ViewModels.Locations"
             xmlns:sfbusy="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             x:DataType="vm:LocationsEditViewModel"
             Title="Location"
             BackgroundColor="White">

    <Grid>
        <ScrollView>
            <StackLayout Padding="16" Spacing="8">

                <!-- Header seguindo padrão EXATO do FamilyEditPage -->
                <StackLayout Spacing="8">
                    <Label Text="{Binding PageTitle}"
                           FontSize="28"
                           FontAttributes="Bold"
                           TextColor="Black"
                           HorizontalOptions="Start" />

                    <Label Text="Location Information"
                           FontSize="14" 
                           TextColor="Gray"
                           Opacity="0.8" />

                    <ProgressBar Progress="{Binding FormCompletionProgress}"
                                ProgressColor="{StaticResource Primary}"
                                BackgroundColor="{StaticResource Gray200}"
                                HeightRequest="4"
                                Margin="0,8,0,0" />
                </StackLayout>

                <!-- Name Field EXATO como FamilyEditPage -->
                <ContentView Style="{StaticResource FormFieldFrameStyle}">
                    <StackLayout Style="{StaticResource FormFieldStackStyle}">
                        <Label Text="Name *" Style="{StaticResource FormFieldLabelStyle}" />

                        <Entry Text="{Binding Name}"
                               Placeholder="Enter location name"
                               FontSize="16"
                               TextColor="Black"
                               BackgroundColor="Transparent"
                               Focused="OnEntryFocused"
                               Unfocused="OnEntryUnfocused" />

                        <Label Text="{Binding NameValidationMessage}"
                               FontSize="12"
                               TextColor="Red"
                               IsVisible="{Binding IsNameValid, Converter={StaticResource InvertedBoolConverter}}" />
                    </StackLayout>
                </ContentView>

                <!-- Location Type Field -->
                <ContentView Style="{StaticResource FormFieldFrameStyle}">
                    <StackLayout Style="{StaticResource FormFieldStackStyle}">
                        <Label Text="Location Type" Style="{StaticResource FormFieldLabelStyle}" />

                        <Entry Text="{Binding LocationType}"
                               Placeholder="e.g., Greenhouse, Window, Outdoor"
                               FontSize="16"
                               TextColor="Black"
                               BackgroundColor="Transparent"
                               Focused="OnEntryFocused"
                               Unfocused="OnEntryUnfocused" />
                    </StackLayout>
                </ContentView>

                <!-- Environment Notes Field -->
                <ContentView Style="{StaticResource FormFieldFrameStyle}">
                    <StackLayout Style="{StaticResource FormFieldStackStyle}">
                        <Label Text="Environment Notes" Style="{StaticResource FormFieldLabelStyle}" />

                        <Editor Text="{Binding EnvironmentNotes}"
                                Placeholder="Light conditions, temperature, humidity, etc."
                                FontSize="16"
                                TextColor="Black"
                                BackgroundColor="Transparent"
                                HeightRequest="60"
                                TextChanged="OnEditorTextChanged" />
                    </StackLayout>
                </ContentView>

                <!-- Description Field EXATO como FamilyEditPage -->
                <ContentView Style="{StaticResource FormFieldFrameStyle}">
                    <StackLayout Style="{StaticResource FormFieldStackStyle}">
                        <Label Text="Description" Style="{StaticResource FormFieldLabelStyle}" />

                        <Editor Text="{Binding Description}"
                                Placeholder="Optional description..."
                                FontSize="16"
                                TextColor="Black"
                                BackgroundColor="Transparent"
                                HeightRequest="80"
                                TextChanged="OnEditorTextChanged" />
                    </StackLayout>
                </ContentView>

                <!-- Configuration Section -->
                <Label Text="Configuration" 
                       FontSize="18" 
                       FontAttributes="Bold" 
                       TextColor="{StaticResource Primary}" 
                       Margin="0,16,0,8" />

                <!-- Settings EXATO como FamilyEditPage -->
                <ContentView Style="{StaticResource FormFieldFrameStyle}">
                    <StackLayout Style="{StaticResource FormSettingsStackStyle}">
                        <Label Text="Settings" Style="{StaticResource FormFieldLabelStyle}" />

                        <!-- Active Toggle -->
                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <Label Grid.Column="0"
                                   Text="🟢"
                                   FontSize="24"
                                   VerticalOptions="Center" />

                            <StackLayout Grid.Column="1" 
                                        Margin="12,0,0,0"
                                        VerticalOptions="Center">
                                <Label Text="Active"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="Black" />
                                <Label Text="Location is available for use"
                                       FontSize="12"
                                       TextColor="Gray" />
                            </StackLayout>

                            <Switch Grid.Column="2"
                                    IsToggled="{Binding IsActive}"
                                    OnColor="{StaticResource SuccessColor}"
                                    ThumbColor="White"
                                    VerticalOptions="Center"
                                    Toggled="OnSwitchToggled" />
                        </Grid>

                        <!-- Favorite Toggle -->
                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <Label Grid.Column="0"
                                   Text="⭐"
                                   FontSize="24"
                                   VerticalOptions="Center" />

                            <StackLayout Grid.Column="1" 
                                        Margin="12,0,0,0"
                                        VerticalOptions="Center">
                                <Label Text="Favorite"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="Black" />
                                <Label Text="Mark as important location"
                                       FontSize="12"
                                       TextColor="Gray" />
                            </StackLayout>

                            <Switch Grid.Column="2"
                                    IsToggled="{Binding IsFavorite}"
                                    OnColor="{StaticResource WarningColor}"
                                    ThumbColor="White"
                                    VerticalOptions="Center"
                                    Toggled="OnSwitchToggled" />
                        </Grid>

                    </StackLayout>
                </ContentView>

                <!-- Action Buttons EXATO como FamilyEditPage -->
                <Grid ColumnDefinitions="*,*" 
                      ColumnSpacing="16"
                      Margin="0,10,0,10">

                    <Button Grid.Column="0"
                            Text="Cancel"
                            Style="{StaticResource CancelButtonStyle}"
                            Clicked="OnCancelButtonTapped" />

                    <Button Grid.Column="1"
                            Text="{Binding SaveButtonText}"
                            Style="{StaticResource DynamicPrimaryButtonStyle}"
                            IsEnabled="{Binding CanSave}"
                            Clicked="OnSaveButtonTapped" />

                </Grid>

                <!-- Save and Add Another Button (only for new location) -->
                <Button Text="Save &amp; Add Another"
                        IsVisible="{Binding ShowSaveAndContinue}"
                        Style="{StaticResource DynamicPrimaryButtonStyle}"
                        IsEnabled="{Binding CanSave}"
                        Margin="0,0,0,0"
                        Clicked="OnSaveAndContinueButtonTapped" />

                <!-- Delete Button (only for edit mode) -->
                <Button Text="Delete"
                        IsVisible="{Binding IsEditMode}"
                        IsEnabled="{Binding CanDelete}"
                        Style="{StaticResource ErrorButtonStyle}"
                        Margin="0,0,0,0"
                        Clicked="OnDeleteButtonTapped" />

            </StackLayout>
        </ScrollView>

        <!-- Loading Overlay EXATO como FamilyEditPage -->
        <ContentView Style="{StaticResource SavingOverlayStyle}" />

    </Grid>

</ContentPage>